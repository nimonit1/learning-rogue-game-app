# DungeonStudy — 設計詳細ドキュメント

設計決定の理由・背景・補足を記録する。「何を決めたか」は `DESIGN.md` を参照。

---

## 1. 背景

### なぜローグライク型を選んだか

従来の暗記アプリ（フラッシュカード等）は受動的な繰り返しが主体で、飽きやすい。一方、ローグライクゲームは「次は何があるか分からない」探索ループと、「強くなっていく」成長実感が内発的動機を継続させる。この2要素が学習継続率の改善に寄与すると判断した。

### なぜ中学3年生をターゲットにしたか

高校受験という明確な目標があり、学習動機が最も高まる時期。また、中学3年生の学習指導要領は範囲が明確で問題コンテンツを設計しやすい。スマートフォンの利用時間が長く、スキマ時間での利用に適している。

### 単一HTMLファイルを選んだ理由

- サーバー・フレームワーク不要でどこでも動作する
- GitHub Pages に push するだけで即公開できる
- 生徒・保護者が「ダウンロードしてオフラインで使う」も可能
- v1 スコープでは開発速度を最優先した判断

---

## 2. 目的

### アクティブリコール統合の根拠

認知科学の「検索練習効果（Testing Effect）」に基づく。情報を「読む・見る」より「思い出そうとする」行為の方が、長期記憶への定着が2〜3倍高い（Roediger & Karpicke, 2006）。問題表示前の3秒想起フェーズは、この効果を最大化するための設計。

### SRS（間隔反復）統合の根拠

Ebbinghaus の忘却曲線によると、一度覚えた知識も時間経過で急速に忘れる。しかし適切な間隔で再学習すると忘却を遅らせられる。「呪いリスト」は不正解問題を追跡し、別セッションで再出題することでSRSを自然に実現する。

---

## 3. 目標

### 1セッション10分設計の根拠

スマートフォンのスキマ時間（通学電車・休憩時間等）を想定。1ダンジョン7問（雑魚6問+ボス1問）で10分程度。短時間で完結することで「今日はクリアできた」という達成感を毎日得られる。

### 管理機能の必要性

デフォルト19ダンジョンは出発点であり、教師・保護者が自校・自家庭向けの問題を追加できることが継続利用の鍵。JSONインポートとAIプロンプト生成機能はこのニーズへの対応。

---

## 4. 前提・制約の詳細

### `window.confirm` 不使用の理由

GitHub Pages での iframe 埋め込みや、Android/iOS の WebView（LINE内ブラウザ等）では `window.confirm` がブロックされ、ダイアログが表示されない既知の問題がある。過去にこの問題で削除操作が無確認で実行される不具合が発生した教訓から、カスタムモーダルに統一した。

### テンプレートリテラル3重ネスト禁止の理由

バッククォートを含む文字列の中にバッククォートが入る3重ネストは、エスケープ漏れによる予期しない構文エラーが発生しやすい。単一ファイルの JavaScript では IDE の構文チェックが効きにくい箇所もあるため、文字列連結方式を徹底する。

### localStorage キーに `_v4` をつけた理由

データ構造の変更時に旧データとの互換性問題を回避するため、バージョンサフィックスを付与している。`_v4` は4回目の構造変更を表す。

---

## 5. 結論（各設計決定の理由）

### 早期レベルアップ優先

Lv1→2 に必要な EXP を 20 に設定し、雑魚1体（EXP=20）で即レベルアップできる。

**理由**: 初プレイで「レベルが上がらない」体験は最大の離脱要因。特に中学生は即時フィードバックへの感度が高く、最初の5分で達成感がないと継続しない。対数カーブにより序盤は急激にレベルが上がり、高レベルになるほど緩やかになるため、ゲームとしての達成感と現実的な上限設計を両立している。

### HP回復3種類の意図的バランス

コンボ（3連続正解でHP+1）・ボス撃破（HP全回復）・呪い解除（失ったHP分回復）の3種類のみ。

**理由**: HP が簡単に回復すると「死」への緊張感がなくなりゲームとしての緊迫感が失われる。一方でHP回復の手段を「正解する」行動に紐付けることで、学習モチベーションと直接連動させた。呪い解除での HP 回復は「苦手問題を克服する」行動への直接的な報酬として機能する。

### ボス封印の設計理由

雑魚（6体）を全滅させるまでボスマスに入れない（⛔表示）。

**理由**: いきなりボス（記述式）に挑むことができると、4択練習なしに高難度問題に直面してゲームオーバーになる。雑魚6体との戦闘でその単元のキーワードを繰り返し思い出してから記述式に挑む、という学習フローを強制することで、アクティブリコールの効果を最大化する。

### ボスを記述式にした理由

difficulty 4（ボス）は4択ではなく自由テキスト入力形式。

**理由**: 4択問題は最悪の場合25%の確率でランダム正解できる。「本当に覚えているか」を測定するには、選択肢なしで自分の言葉でアウトプットできることを確認する必要がある。記述式は認知科学的にも「自由再生」として最も高い記憶定着効果を持つ。ただし完全一致判定であるため、問題作成時の `answer` 表記を統一することが運用上の注意点。

### 呪いリストの設計理由

不正解 / タイムオーバーの問題を `CURSE_LIST` に記録し、ダンジョン入場時に1問自動出題。

**理由**: SRS の核心は「忘れかけたタイミングで思い出す」こと。次のダンジョン入場時に呪い問題が出題されることで、自然な間隔での再想起が実現する。ゲーム用語「呪い」は、苦手問題を「呪いを解く」という動機付けに転換する心理的設計。HP損失分を回復できるという直接報酬により、プレイヤーが積極的に苦手問題に挑む行動を誘発する。

---

## 6. リスク・課題の詳細

### 記述式正答判定の改善案（未実装）

**現状の問題**:
- `c.trim() === en.ans.trim()` の完全一致のみ
- 「x = 4」と「x=4」、「ポツダム宣言」と「ポツダム宣言（1945年）」が不一致

**検討中の改善案**:
1. 全角半角正規化: `str.replace(/[Ａ-Ｚａ-ｚ０-９]/g, s => String.fromCharCode(s.charCodeAt(0)-0xFEE0))`
2. スペース除去: `str.replace(/\s/g, '')`
3. 複数正解対応: `answer` フィールドを配列で受け付ける
4. AI判定（将来）: サーバーサイドで LLM を使った意味的正誤判定

**運用上の回避策**: 問題作成時に `answer` の表記を「スペースなし・半角」で統一するようAIプロンプトに指示を含める。

### SRS非永続化の影響と対策

**現状**: `CURSE_LIST` はメモリ上にのみ存在し、ページリロードで消える。

**影響**: ブラウザを閉じると苦手問題の記録が失われ、SRS効果が不連続になる。

**暫定対策**: `localStorage` に `CURSE_LIST` を保存する実装が比較的容易（`saveDungeons()` に追加）。ただし永続的なユーザーアカウントへの紐付けはバックエンド化が必要。

---

## 7. 詳細

### アクティブリコール統合の実装詳細

**想起フェーズ（3秒）の設計意図**:
問題が表示される前に3秒のカウントダウンを表示し、「頭の中で答えを思い浮かべてから」問題を確認させる。この「予測→確認」のプロセス自体が記憶の強化につながる（予測符合化効果）。問題を見てから考えるのではなく、見る前から思考を始める習慣を育てる。

**20秒タイマーの根拠**:
- 10秒未満：考える前に時間切れになる
- 30秒以上：待機時間が長すぎてゲームのテンポが失われる
- 20秒：「真剣に考えたが思い出せなかった」体験を最大化する最適値
- 速攻ボーナス（10秒以内）：即座に思い出せた場合の報酬

### Canvas アイソメトリック描画の設計

**等角投影（Isometric）を採用した理由**:
真上から見る2Dより空間の奥行きが表現できる。単純なスプライトではなく手書き感のあるタイル描画にすることで「ダンジョン探索」の雰囲気を演出。

**描画定数（`OBL`）**:
```js
TW = 40   // タイル幅（ピクセル）
TH = 24   // タイル高（ピクセル）
WH = 26   // 壁の高さ（ピクセル）
SPR = 28  // スプライトサイズ（ピクセル）
```

**3パス描画の理由**:
1. パス1: 床タイル（最背面）
2. パス2: 壁タイル（床の上に重ねる）
3. パス3: キャラクター・敵・アイテム（最前面）

深度ソート（Y座標降順）なしで擬似的な奥行き表現を実現するために、描画順序を厳密に管理している。

### 難易度バランスの思考プロセス

| difficulty | ゲーム内 | EXP | 出題形式 | 設計意図 |
|---|---|---|---|---|
| 1 | 雑魚 tier0 | 20 | 4択 | 基本知識の確認。失敗コストが低い |
| 2 | 雑魚 tier1 | 28 | 4択 | 応用問題。EXP増で挑戦への意欲を促す |
| 3 | 雑魚 tier2 | 38 | 4択 | 発展問題。ボス前の最終確認 |
| 4 ボス | ボス | 50 | 記述式 | 真の定着測定。撃破でHP全回復 |

ボスのEXPが最大だが、HP全回復という大きな報酬があるため EXP だけでボスの価値を測るべきではない。

### 初期ダンジョン構成の選定理由

**中学3年生に特化した理由**:
- 高校受験という期限があり、学習動機が最も高まる時期
- 全5教科（国数英理社）の中3範囲が明確に定義されている
- 問題難易度の設定がしやすい（入試問題の難易度分布が参考になる）

**各ダンジョン7問（雑魚6+ボス1）の根拠**:
- 雑魚6体 × 約80秒/問 = 約8分
- ボス1問 = 約2分（記述式・考える時間を含む）
- 合計 10分程度のセッション設計に合致
- ボス封印には最低1体の雑魚が必要だが、単元の重要概念を網羅するには6問が妥当

### AIプロンプト生成機能の位置づけ

`generatePrompt()` は教師・保護者が新しいダンジョンを作成する際の補助ツール。

**生成プロセス**:
1. 管理画面で科目・問題数・難易度・単元を指定
2. プロンプト文字列を生成してクリップボードにコピー
3. Claude・ChatGPT 等の AI にペーストして JSON を生成
4. 生成された JSON を管理画面のインポート機能で貼り付け
5. `sanitizeJSON()` が前処理（Markdownコードブロック除去、全角文字変換、trailing comma除去）を自動実施

**`sanitizeJSON()` が処理する主なケース**:
- AIが出力するMarkdownのコードブロック（` ```json ... ``` `）の除去
- 全角の括弧・引用符（「"」→ `"` 等）の変換
- JSONの末尾カンマ（trailing comma）の除去
- BOM（バイト順マーク）の除去

### 今後の優先事項の根拠

1. **問題DBのバックエンド化**: localStorage の5MB上限と SRS 非永続化を同時解決できる最大インパクトの改善
2. **SRS永続化**: 学習効果の継続性に最も直接的に寄与。ユーザーアカウントが必要
3. **記述式正答判定の改善**: 不当な不正解によるユーザー体験の悪化を解消
4. **コンポーネント分割**: 1521行の単一ファイルは機能追加・デバッグの障壁になりつつある
5. **問題コンテンツ拡充**: AIプロンプト生成機能があるため比較的容易に実施可能
