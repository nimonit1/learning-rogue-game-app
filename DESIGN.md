# DungeonStudy — 設計ドキュメント

## 1. 背景

ローグライク型の学習ゲーム `dungeon_study.html`。単一HTMLファイル、外部依存なし。ブラウザで開くだけで動作する。

- **ターゲット**: 中学3年生（12〜15歳）、スマートフォン優先
- **プラットフォーム**: スマートフォン優先のHTML単一ファイル
- **公開状況**: GitHub Pages で公開済み

---

## 2. 目的

アクティブリコール（能動的想起）と SRS（間隔反復）をローグライク型ゲームシステムに統合し、中学3年生が継続的に学習習慣を身につけられる教育アプリを提供する。

---

## 3. 目標

- 中学3年生が楽しみながら高校入試対策できるWebアプリ
- 1ダンジョン10分程度で完結する短時間セッション設計
- スマートフォンのブラウザで追加インストールなしで動作
- 保護者・教師がJSON形式で問題を追加・編集できる管理機能

---

## 4. 前提・制約

| 制約 | 内容 |
|---|---|
| 単一HTMLファイル | 外部ライブラリ・サーバー不要。`dungeon_study.html` のみ |
| localStorage 使用 | キー: `dungeon_study_dungeons_v4`、上限 5MB |
| ファイル名固定 | `dungeon_study.html` のまま変更しない（URL変更なし） |
| `window.confirm/alert/prompt` 使用禁止 | iframe / WebView 非対応のためカスタムモーダルで代替 |
| テンプレートリテラルの3重ネスト禁止 | 構文エラーのリスクがあるため文字列連結方式を使用 |
| マップサイズ上限 | `MSIZE=9`（9×9グリッド）。13以上にする場合は反復版 Backtracking に変更要 |

---

## 5. 結論（主要設計決定）

| # | 決定内容 | 概要 |
|---|---|---|
| 1 | **早期レベルアップ優先** | Lv1→2 を雑魚1体で達成できる EXP 設定。序盤達成感が継続率に直結 |
| 2 | **HP回復3種類** | コンボ（3連続正解）・ボス撃破・呪い解除の3種類に限定。意図的なバランス設計 |
| 3 | **ボス封印** | 雑魚全滅が前提。アクティブリコール練習後にボス戦へ進む学習フロー |
| 4 | **ボスは記述式** | 4択の運任せを排除し、真の定着度を測定する |
| 5 | **呪いリスト（SRS）** | 不正解問題を記録し、間を置いて再想起させる。HP損失分の回復が報酬 |
| 6 | **ファイル名固定** | バージョンは `APP_VERSION` 定数で管理。タイトル画面に表示 |
| 7 | **カスタムモーダル** | `window.confirm` を使わず、独自モーダルUIで代替 |
| 8 | **文字列連結** | テンプレートリテラルのネストを避け、文字列連結方式を採用 |

---

## 6. リスク・課題

| リスク | 内容 | 対応方針 |
|---|---|---|
| 正答判定の揺れ | ボス記述式は `c.trim() === en.ans.trim()` の完全一致。「x = 4」と「x=4」が不一致になる | 全角半角正規化・スペース除去（v1.1 以降） |
| SRS 非永続化 | `CURSE_LIST` はページリロードでリセットされる | バックエンド化（優先事項 #2） |
| localStorage 上限 | 5MB。問題数が数百問規模になると不足 | 別途 DB・API 検討（優先事項 #1） |
| 問題コンテンツ不足 | 現在19ダンジョン133問 | AIプロンプト生成機能で拡充 |

---

## 7. 詳細

### ゲームシステム設計

| 機能 | 仕様 |
|---|---|
| マップ | 9×9グリッド、Recursive Backtracking で全マス連結保証 |
| 霧（フォグオブウォー） | 未探索エリア暗転、2マス範囲を自動開示 |
| 描画方式 | Canvas、アイソメトリック2D（斜め45°見下ろし）|
| アバター | 8種類（戦士/魔法使い/弓使い/騎士/フォックス/ウルフ/竜騎士/妖精） |

### レベル・EXP 設計

| 項目 | 値 |
|---|---|
| レベル上限 | Lv.9999 |
| EXP計算式 | `expNeeded(lv) = floor(15 * ln(lv+1) + 10)` |
| Lv1→2 必要EXP | 20（雑魚1体で到達） |
| difficulty別EXP | `{1:20, 2:28, 3:38, 4:50}` |
| 速攻ボーナス | 制限時間50%以内の正解 → EXP満額、超過 → 60% |
| コンボ回復 | 3連続正解 → HP+1 |

### 戦闘フロー

1. 敵遭遇 → 想起フェーズ（3秒カウントダウン）
2. 問題表示 → 20秒タイマー開始
3. 4択クリック（雑魚）または テキスト入力（ボス）
4. 正誤判定 → EXP付与 / HP-1

### 正誤判定

| 問題形式 | 判定方法 |
|---|---|
| 4択（雑魚） | `c === en.ans`（完全一致） |
| 記述式（ボス） | `c.trim() === en.ans.trim()` |

### 呪いリスト（SRS）

- 不正解 / タイムオーバー → `addCurse(en, hpLost)` で `CURSE_LIST` に追加
- ダンジョン入場時に呪い問題を1問自動出題
- 呪い正解 → 失ったHP分回復 + 呪い解除
- 不正解後に「🔄 再挑戦」か「🏃 逃げる」を選択可能

### 称号システム（14段階）

- Lv.1「見習い冒険者」〜 Lv.9999「全知の存在」

### JSONフォーマット（外部インポート用）

```json
{
  "name": "ダンジョン名",
  "subject": "科目",
  "icon": "⚗️",
  "color": "#00809E",
  "questions": [
    {
      "difficulty": 1,
      "monster_name": "敵名",
      "question": "問題文",
      "choices": ["A", "B", "C", "D"],
      "answer": "正解",
      "explanation": "解説"
    },
    {
      "difficulty": 4,
      "type": "boss",
      "monster_name": "ボス名",
      "question": "問題文",
      "answer": "正解",
      "explanation": "解説"
    }
  ]
}
```

- `difficulty 1〜3`：4択問題（雑魚 tier0/1/2）
- `difficulty 4 + type:"boss"`：記述式（ボス、choices なし）
- `explanation`：正解・不正解問わず表示
- `monster_name`：省略可（省略時は「敵N」「ボス」）

### 内部データ構造

```js
{
  id: "d_1234_abcd",
  name: "ダンジョン名",
  subject: "科目",
  icon: "⚗️",
  color: "#00809E",
  enemies: [
    {
      id: "d_1234_abcd_q0",
      name: "敵名",
      tier: 0,        // 0 / 1 / 2
      exp: 20,        // difficulty → EXP 変換済み
      difficulty: 1,
      isBoss: false,
      q: "問題文",
      ans: "正解",
      choices: ["A","B","C","D"],  // ボスは undefined
      explanation: "解説"
    }
  ]
}
```

### 初期ダンジョン構成

19ダンジョン・133問（各ダンジョン 雑魚6問 + ボス1問）。中学3年生学習指導要領準拠。

| 教科 | ダンジョン数 | 主要単元 |
|---|---|---|
| 理科 | 5 | イオン・遺伝・天体・エネルギー・進化 |
| 数学 | 4 | 平方根・二次方程式・二次関数・図形 |
| 英語 | 3 | 語彙・文法・長文読解 |
| 社会 | 4 | 近現代史・公民・経済・国際社会 |
| 国語 | 3 | 漢字・文学・古典 |

### バージョン管理方針

| 項目 | 内容 |
|---|---|
| 定数 | `APP_VERSION = "1.0.0"` |
| 表示場所 | タイトル画面（「解く。倒す。覚える。」の直下、薄いグレー） |
| ルール | メジャー（大機能追加）/ マイナー（問題追加・機能改善）/ パッチ（バグ修正） |
| ファイル名 | `dungeon_study.html` 固定。更新時もファイル名は変えない |

### 主要定数一覧

```js
APP_VERSION = "1.0.0"
MSIZE       = 9
LS_KEY      = "dungeon_study_dungeons_v4"
DIF_EXP     = {1:20, 2:28, 3:38, 4:50}
DIF_TIER    = {1:0, 2:1, 3:2, 4:2}
```
